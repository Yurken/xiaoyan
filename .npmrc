public-hoist-pattern[]=*expo*
public-hoist-pattern[]=*babel*
public-hoist-pattern[]=*@babel*
public-hoist-pattern[]=expo-asset
public-hoist-pattern[]=babel-preset-expo
public-hoist-pattern[]=@react-native*
public-hoist-pattern[]=react-native*
# 不再全局 hoist @types/*：workspace 里 desktop/web 用 React 19、mobile 用 React 18，
# 全局 hoist 会导致 mobile 的 TypeScript 解析到 root 的 @types/react@19，与 React Native 类型冲突。
# 各包按需安装自己的 @types 即可。
